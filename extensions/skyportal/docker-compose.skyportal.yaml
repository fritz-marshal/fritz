version: "3.7"

services:
  web:
    extends:
      file: docker-compose.yaml
      service: web
    volumes:
      # fritz takes care of making a docker-appropriate config.yaml
      - config.yaml:/skyportal/config.yaml

  db:
    extends:
      file: docker-compose.yaml
      service: db

volumes:
  dbdata:
